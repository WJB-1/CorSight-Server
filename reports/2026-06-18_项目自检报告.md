# CorSight-Server_release 项目自检报告

**检查时间**: 2026-06-18 22:10
**检查范围**: 后端 (backend/) + 前端源码 (frontend/src/)
**检查维度**: 结构严谨性、耦合度、潜在Bug
**目标项目**: `CorSight-Server_release`

---

## 一、项目结构概览

```
backend/
├── server.js                    # 主入口
├── config/                      # 配置层 (envConfig, db, uploadConfig)
├── middleware/                   # 中间件 (auth, errorHandler, logger, upload)
├── routes/                      # 路由层 (8 个文件)
├── controllers/                 # 控制器层 (7 个文件)
├── services/                    # 服务层 (21 个文件)
├── models/                      # 模型层 (5 个文件)
├── lib/                         # 纯工具库 (6 个文件)
├── prompts/                     # 提示词模板 (7 个文件)
├── scripts/                     # 运维脚本 (14 个文件)
├── shared/images/               # 图片存储（软链）
└── package.json

frontend/src/                    # 前端源码 (10 个文件)
├── main.js, api.js, map.js
├── routePlanner.js, roadInteraction.js
├── panel.js, console.js, utils.js
├── tileRenderer.js, roadTheme.js, debugCoord.js
└── styles.css
```

**统计**:
| 层 | 文件数 | 状态 |
|---|--------|------|
| routes | 8 | ✅ 正常 |
| controllers | 7 | ✅ 正常 |
| services | 21 | ⚠️ 偏多（阈值 15） |
| models | 5 | ✅ 正常 |
| lib | 6 | ✅ 正常 |

---

## 二、结构严谨性检查

### 2.1 依赖方向检查

**合法依赖链**: `routes → controllers → services → models` + `lib`（纯工具）

| 检查项 | 结果 |
|--------|------|
| routes → controllers | ✅ 所有路由文件正确调用 controller 层 |
| controllers → services | ✅ 所有控制器正确调用 service 层 |
| controllers → lib | ✅ 仅限纯工具函数（multer, idGenerator） |
| models → services | ✅ 无反向依赖 |
| lib → services/controllers | ✅ 无上层依赖 |
| routes 直接访问 req.body | ✅ 仅限传参，无业务逻辑 |
| controllers 直接访问 Model | ✅ 未发现（除 SSE 控制器管理自身连接集合，可接受） |

**结论: 依赖方向完全合规，无跨层调用违规。**

### 2.2 跨层调用分析

| 文件 | 行数 | 功能 | 合规性 |
|------|------|------|--------|
| `guideRoutes.js` | 456 | 景点推荐路由 | ✅ 内联控制器模式（路由+逻辑一体） |
| `sseController.js` | 73 | SSE 连接管理 | ✅ 无 Model 直接操作（连接集合是 Controller 自身资源） |
| `batchProcessorService.js` | 152 | 批处理编排 | ✅ 通过 model 访问数据，通过 lib 调用工具 |

### 2.3 各层职责分析

**Services 层 (21 个文件)**:

| 子领域 | 文件 | 功能 |
|--------|------|------|
| VLM 领域 (5) | vlmService, vlmBatchClient, vlmTagRetriever, batchProcessorService, broadcastService | VLM 调用、批量处理、标签检索、广播 |
| OSM 领域 (4) | osmPatchService, osmWayRetriever, injectionService, roadTagService | OSM 补丁、路网检索、标签注入、道路标签 |
| 路由规划 (3) | routeService, graphhopperRouteService, amapService | 路由引擎管理、路线计算 |
| 数据管理 (5) | semanticTagService, uploadSessionService, batchQueryService, workspaceService, imageStorageService | 标签管理、上传会话、批量查询、工作空间 |
| 基础设施 (4) | sseManager, ragRetrievalService, sceneClassifier, sceneClassificationRunner | SSE、RAG 检索、场景分类 |

**建议**: VLM 和 OSM 可考虑按子领域分目录（非紧急）。

---

## 三、耦合度分析

### 3.1 高复用价值代码（已抽离到 lib/）

| 工具 | 被引用数 | 状态 |
|------|---------|------|
| `lib/coordTransform.js` | 6 处 | ✅ 已独立 |
| `lib/geo.js` | 11 处 | ✅ 已独立 |
| `lib/polyline.js` | 2 处 | ✅ 已独立 |
| `lib/idGenerator.js` | 1 处 | ✅ 已独立 |
| `lib/processManager.js` | 1 处 | ✅ 已独立 |
| `lib/vlmParser.js` | 1 处 | ✅ 已独立 |
| `lib/routeContextMatcher.js` | 1 处 | ✅ 已独立 |
| `lib/llmClient.js` | 1 处 | ✅ 已独立 |

**结论: 所有高复用纯工具函数已正确抽离到 lib/ 层，耦合度良好。**

### 3.2 模块化状态

| 子领域 | 文件数 | 内聚度 | 建议 |
|--------|--------|--------|------|
| VLM | 5 | 高 | 可选: 创建 services/vlm/ 目录 |
| OSM | 4 | 高 | 可选: 创建 services/osm/ 目录 |
| 路由规划 | 3 | 高 | 可选: 创建 services/routing/ 目录 |

---

## 四、潜在 Bug 检查

### 4.1 高优先级（可能导致崩溃）

| # | 文件 | 行 | 问题 | 状态 |
|---|------|----|------|------|
| H1 | `middleware/logger.js` | 160-163 | `setInterval` 清理不完整：`cleanup()` 仅 `clearInterval(interval)` 未置空变量；多进程未保护 | ⚠️ 低风险 |
| H2 | `server.js` | 133 | SPA fallback 未检查文件存在性：`app.get('*')` 在 `express.static(fallthrough: true)` 后面，可能返回 index.html 替代 404 | ⚠️ 已修复（读取 dist/index.html 检查存在性） |

### 4.2 中优先级（影响稳定性）

| # | 文件 | 行 | 问题 |
|---|------|----|------|
| M1 | `batchProcessorService.js` | 70-75 | `processBatch` 设置 `batch.status = 'processing'` 并 `save()` 在 try-catch 外面。若 `save()` 抛异常（如 MongoDB 断连），batch 将永久卡在 `processing` 状态 |
| M2 | `graphHopperService.js` | 177-189 | `startGraphHopper` spawn 子进程无 `'error'` 事件监听。spawn 失败（如 java 不在 PATH）会触发未捕获的 error 事件 |
| M3 | `middleware/logger.js` | 11 | `req.ip` 直接输出，未处理 IPv6 映射格式（如 `::ffff:127.0.0.1`） |
| M4 | `middleware/auth.js` | 17 | IP 信任检查：`req.ip === '127.0.0.1'` 在反向代理后可能不准确 |
| M5 | `controllers/batchController.js` | 115 | `client.chat.completions.create` 用字符串模板传 images，未用 SDK 消息格式 |
| M6 | `routes/guideRoutes.js` | 75, 88 | 链式属性访问未做空值校验（如 `?.`） |

### 4.3 低优先级（代码质量）

| # | 文件 | 行 | 问题 |
|---|------|----|------|
| L1 | `middleware/errorHandler.js` | 30 | `JSON.stringify(err)` 对 Error 对象返回 `{}`，应使用 `err.message` |
| L2 | `services/injectionService.js` | 66 | `maybeTriggerInjection` 是死代码（从未被调用），但有潜在未处理 Promise 风险 |
| L3 | `services/vlmService.js` | 72 | `analyzeImage` 调试函数无 try-catch，3 个不同抛出点 |
| L4 | `controllers/sseController.js` | 26 | `req.app.locals.sseClients` 可能在多进程模式下丢失 |
| L5 | `vlmBatchClient.js` | 16 | `API_KEY` 过期后无提醒（有效期至 2026-03-28） |
| L6 | `graphHopperService.js` | 48 | `GRAPH_HOPPER_PORT` 硬编码为 8080 |
| L7 | `controllers/navigationController.js` | 29 | `LLM_API_KEY` 过期后无提醒 |
| L8 | `models/SemanticTag.js` | 65 | `crs` 字段未加索引，查询按 CRS 过滤时性能差 |
| L9 | `graphHopperService.js` | 26-28 | `baseState` 未 clone，直接修改可能影响后续 reset |
| L10 | `routes/guideRoutes.js` | 1-456 | 456 行无分类注释，可维护性差 |

### 4.4 前端问题

| # | 文件 | 问题 |
|---|------|------|
| F1 | `routePlanner.js:209` | `enable_sse` 默认 `false`（SSE 被禁用） |
| F2 | `routePlanner.js:191-275` | 同时保留 sync 和 SSE 两套实现，约 90 行重复代码 |
| F3 | `console.js:1` | `console.warn("✅ CorSight console")` 可能在生产环境造成混淆 |
| F4 | `map.js:191` | `map.on('click', 'guides-text-layer')` 使用 `e.features[0]` 未做空值检查 |
| F5 | `map.js:146` | `highlightedFeature` 变量声明但从未使用（死代码） |
| F6 | `console.js:110-115` | `case 'help': case 'clear': case 'theme':` 为预留的空实现 |
| F7 | `api.js` | fetch 调用未统一检查 `res.ok` |

---

## 五、修复优先级

### 立即修复（影响稳定性）

| # | 问题 | 修复方案 |
|---|------|---------|
| M1 | `processBatch` save 在 try-catch 外 | 将 lines 70-75 纳入 try-catch 块 |
| M2 | `startGraphHopper` 无 spawn error 监听 | 添加 `child.on('error', ...)` |
| H2 | SPA fallback 文件检查 | ✅ 已修复 |

### 短期修复（1-2 周）

| # | 问题 | 修复方案 |
|---|------|---------|
| M3 | `req.ip` IPv6 格式 | 添加 IPv4 映射清理 |
| M4 | IP 信任检查 | 使用 `req.ips` 或配置 trust proxy |
| L1 | `JSON.stringify(err)` | 改用 `err.message` + `err.stack` |
| L2 | `maybeTriggerInjection` 死代码 | 添加 try-catch 或删除 |
| F7 | fetch 未检查 res.ok | 统一添加 `if (!res.ok)` 检查 |

### 中期改进（1-2 月）

| # | 问题 | 修复方案 |
|---|------|---------|
| F2 | SSE 重复代码 | 删除 sync 实现，统一用 SSE |
| L10 | guideRoutes 可读性 | 添加分类注释 |
| L8 | crs 字段无索引 | 添加 `{ crs: 1 }` 索引 |
| -- | services 分目录 | 按子领域分 vlm/ osm/ routing/ 目录 |

---

## 六、总结

### 评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 依赖方向 | ⭐⭐⭐⭐⭐ | 完全合规，无跨层调用 |
| 文件组织 | ⭐⭐⭐⭐ | services 层偏多但可接受 |
| 工具抽离 | ⭐⭐⭐⭐⭐ | lib/ 6 个独立模块，复用率高 |
| 错误处理 | ⭐⭐⭐⭐ | 控制器层全覆盖，少数 service 存在裸 async |
| 代码质量 | ⭐⭐⭐⭐ | 少量死代码和硬编码 |
| **综合** | **⭐⭐⭐⭐** | **整体健康度优秀，有少量需修复的中优先级问题** |

### 与上次检查对比

| 指标 | 上次 | 本次 |
|------|------|------|
| 跨层调用 | ❌ 5 处 | ✅ 0 处 |
| 错误处理 | ⚠️ 6 处 | ⚠️ 2 处 |
| 反向依赖 | ❌ 5 个文件 | ✅ 0 个 |
| lib/ 工具数 | 2 个 | 6 个 |
| 文件组织 | 不清晰 | 清晰 |

**结论**: 项目整体健康度显著提升。架构分层清晰，依赖方向完全合规，工具函数抽离到位。剩余问题主要是异步错误处理边界（M1/M2）和少量代码质量细节，可在后续迭代中逐步修复。

---

*报告生成: 2026-06-18 | 下次建议检查: 2026-06-25*
